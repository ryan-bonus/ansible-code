name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u ryan bash -c 'cat > /home/ryan/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/ryan/ansible/inventory.yaml
          roles_path    = /home/ryan/ansible/roles
          host_key_checking = False
          remote_user = ryan
          private_key_file = /home/ryan/.ssh/id_rsa
          log_path = /home/ryan/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/ryan/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/ryan/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown ryan:ryan /home/ryan/ansible/ansible.cfg
          sudo chmod 644 /home/ryan/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u ryan ls -la /home/ryan/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u ryan cat /home/ryan/ansible/ansible.cfg
